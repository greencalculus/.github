## What changed

<!-- One or two sentences. If this fixes something that was wrong, say what it
     was doing before and why that was wrong. -->

## Why

<!-- The problem, not the patch. Link an issue if there is one. -->

## How it was verified

<!-- What you actually ran, and what it printed. "Tests pass" is weaker than the
     command and its output. If a published number changes, show the before and
     after. -->

## Checklist

- [ ] Tests pass locally
- [ ] No new runtime dependencies (client libraries are dependency-free)
- [ ] If a factor, value or count changed, its source and verification link still resolve
- [ ] If this is a release, the version in the manifest is bumped — that is what publishes it
